
## COUNT(), AVG() and SUM()

COUNT() Syntax
```roomsql
SELECT COUNT(column_name)
FROM table_name
WHERE condition;
```


AVG() Syntax
```roomsql
SELECT AVG(column_name)
FROM table_name
WHERE condition;
```

SUM() Syntax
```roomsql
SELECT SUM(column_name)
FROM table_name
WHERE condition;
```